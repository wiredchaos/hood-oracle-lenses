/**
 * AKASHIC LENSES - Reading Card Video Composition (stub)
 *
 * Renders a 9:16 animated card for Instagram/TikTok story sharing.
 * Activate by installing Remotion — see remotion/index.tsx for instructions.
 *
 * When implemented, this composition will:
 * - Animate in the SpiralMedallion (rotation reveal)
 * - Display the user's archetype, life path, and key numbers
 * - Fade through lens layers over ~6 seconds
 * - Export as a 1080×1350 MP4 (or GIF via FFmpeg)
 */

// import { AbsoluteFill, useCurrentFrame, useVideoConfig, spring } from "remotion";
// import type { Reading } from "../../src/lib/lenses";

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface ReadingCardProps {
  // reading: Reading;
}

export const ReadingCard = (_props: ReadingCardProps) => {
  // const frame = useCurrentFrame();
  // const { fps } = useVideoConfig();
  return null; // implement when Remotion is installed
};
